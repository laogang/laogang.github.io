  

```
   在特定的场景下，排序条件的字段是中文或者没有可参考规则的情况下，使用mysql提供的 filed(排序字段，排序规则)进行排序。

eg: order by 
              isnull(leave)
              ,filed(leave,'违约类','风险类','关注类','正常类')

ps: isnull() 函数 为空则返回1，非空则返回0，保证自定义排序时空值排在后面。

```


